package org.impl;

import org.interfaces.ValueContainer;

import java.util.ArrayList;

public class ManyValues extends ArrayList<Integer> implements ValueContainer {
    // ArrayList already implements Iterable<Integer>
}
